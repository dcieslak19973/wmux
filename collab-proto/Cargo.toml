[package]
name = "collab-proto"
version = "0.1.0"
edition = "2021"
description = "Wire protocol shared between wmux and the collab rendezvous server."
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
