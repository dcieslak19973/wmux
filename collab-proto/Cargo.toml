[package]
name = "collab-proto"
version = "0.1.0"
edition = "2021"
description = "Wire protocol shared between wmux host and wmux/PWA viewer."
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
